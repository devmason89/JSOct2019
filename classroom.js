//* PEER CODING CHALLENGE
//* Create 3 conditional operators to check the type of a variable.

//String
//Number
//Boolean

let example = 9

switch (typeof example) {
    case 'string':
    console.log('This is a string');
    break;
    case 'number':
    console.log('This is a number');
    break;
    case 'boolean':
    console.log('This is a boolean');
    break;
}

(typeof === example)

//Find these notes on Slack about the challenge for string, boolean, and number. 
// All of these solutions will be published in modules on canvas.

if (typeof === 'string') ? console.log('This is a string') : (typeof example === 'number') ? console.log("")

let count = 0;
for ( ;  count < 9; ++count ) {
  console.log(count);
}