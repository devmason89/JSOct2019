//* Literals

//* JavaScript defines two types of values: Fixed values and variable values.
//* Fixed values: literals
//* Variable values: variables

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString());//method
console.log(days.length);//property

let tired = true;

let soup = {                      //object
    a: 'chicken noodle',
    b: 'tomato',
    c:  'beef and barley',
}
console.log(soup.a.length);
console.log(typeof soup.c);          //property
console.log(soup.c.toUpperCase());

let car = "Honda";

let dec = 10.2; //Also an example of a literal